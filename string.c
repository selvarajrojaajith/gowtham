
#include <stdio.h>
using namespace std;
 
int count(string s, int K)
{
    int n = s.length();
    for (int i = 0; i < n; i++) {
     {
     c=c+;
        }
}
    return C * K + (K * (K - 1) / 2) * c1 * c2;
}
int main()
{
    string S = "abcb";
    int k = 2;
    cout << countOccurrences(S, k) << endl; 
    return 0;
}
